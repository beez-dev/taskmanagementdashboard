# Application Layer

The **use cases** of this product. One class or function per user intent. Framework-agnostic pure TypeScript; a use case never renders anything and never opens a socket.

## What lives here

- **Use cases** — `CreateTask`, `AssignTask`, `ListTasksForBoard`. 
